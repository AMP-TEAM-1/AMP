services:
  backend:
    build:
      context: .                  # ✅ 빌드 기준을 AMP 루트로
      dockerfile: ./app/api/Dockerfile   # ✅ Dockerfile이 app/api 안에 있으니 이렇게 지정
    env_file:
      - .env                      # AMP/.env
    ports:
      - "8000:8000"

